import './navbar.css';

const Navbar = () => {
    return (
        <div className={'navbar'}>
            <a className={'navbar-home'} href={'/'}>Adrian Galus</a>
        </div>
    )
}

export default Navbar;